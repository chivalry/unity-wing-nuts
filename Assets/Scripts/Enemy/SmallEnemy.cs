namespace WingNuts.Enemy
{
    // 1 HP, fires every 2 s, no special AI beyond EnemyBase.
    // Set maxHp=1, fireInterval=2, scoreValue=100 in the prefab Inspector.
    public class SmallEnemy : EnemyBase { }
}
